# POL-9 implementation plan

> Execute inline using executing-plans and test-driven-development; request an independent final code review. AGENTS.md authorizes routine delivery through PR without additional design gates.

**Goal:** Deliver a French static shell with extensible, data-generated navigation.
**Architecture:** POL-8 public snapshot → validated catalog → generic Astro routes; isolated fictional demo output.
**Tech stack:** Node 24, locked Astro/TypeScript, plain CSS, Node tests, development-only Playwright.
**Spec:** ../specs/2026-09-09-pol-9-site-design.md

## Constraints

English code/docs; French UI; no real draft/private data, paid calls, visitor scripts or deployment. Stable ID URLs and separate exact-version historical URLs. Only active public records enter the catalog. Keep POL-8 tests and contracts intact.

- [x] Catalog and boundary: add `src/site/catalog.ts`, `load.ts`, route helpers, production empty snapshot and sanitized demo fixtures. Tests first: private input/fixtures/broken refs fail, current records exclude historical versions, adding extension changes indexes without old URL changes. Interface: `createCatalog(input, allowFixtures)` returns validated `data`, current collections and `resolve(collection, ref)`; `recordPath(collection, record, historical)` returns a stable URL.
- [x] Static pages: configure Astro, create shared layout/navigation/empty-state components and generic questions/candidates/topics/sources routes plus method/home/404 pages. Add integration tests first asserting generated routes, French language, demo warning, existing links, no private values or client scripts. Build with `npm run build`, then base/extended demo modes.
- [x] Verification and handoff: add local browser keyboard/mobile tests and byte reporting; update CI/install/check docs and AGENTS.md. Run locked install, existing+new tests, Astro checks, both demo builds, production build and link/asset checks. Review the complete diff and independent findings, delivery proceeds through one POL-9 commit/push/PR under the repository workflow. No merge or Linear writes without new authorization.
